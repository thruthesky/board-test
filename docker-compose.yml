services:
  board:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80
    volumes:
      - board-data:/var/www/html/data
      - board-uploads:/var/www/html/uploads

volumes:
  board-data:
  board-uploads:
